<%= form_with(model: name_work, local: true) do |form| %>
  <% if name_work.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(name_work.errors.count, "error") %> prohibited this name_work from being saved:</h2>

      <ul>
      <% name_work.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
